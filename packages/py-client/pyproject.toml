[project]
name = "visiovox-client"
version = "0.1.0"
description = "Generated Python API client"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/visiovox_client"]
