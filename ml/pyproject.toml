[project]
name = "visiovox-ml"
version = "0.1.0"
description = "SEAVE model, pipeline stages, training and evaluation"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["seave", "pipeline", "eval", "training"]
